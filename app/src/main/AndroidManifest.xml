<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Firebase Auth/Firestore/Storage requieren internet. -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Ubicacion: la usa el Home (centrar mapa en el usuario) y el picker de crear evento. -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <!-- Camara: la usa el escaner de QR (del organizador del evento). -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <uses-feature android:name="android.hardware.camera.autofocus" android:required="false" />

    <!-- Notificaciones push (FCM). Requerido a partir de Android 13 (API 33). -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:name=".UnityEventsApp"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.UnityEvents"
        tools:targetApi="34">

        <!-- Clave API de Google Maps. El valor se inyecta desde local.properties via manifestPlaceholder. -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="${MAPS_API_KEY}" />

        <!-- Canal por defecto de FCM. Notificaciones que no traen channel_id explicito caen aqui. -->
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_channel_id"
            android:value="@string/fcm_default_channel_id" />

        <!-- Servicio de FCM: escucha tokens y mensajes entrantes. -->
        <service
            android:name=".core.service.UnityEventsFcmService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

        <!--
            La Activity arranca con el tema Splash (Theme.UnityEvents.Splash) que muestra
            el logo sobre fondo blanco mientras Application se inicializa. Luego MainActivity
            llama installSplashScreen() y cambia al tema normal Theme.UnityEvents.
        -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.UnityEvents.Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
